%h1= @tag

%table
  - for user_tagging in @user_taggings
    %tr
      %td.linkages_count= user_tagging.qty
      %td.title= link_to user_tagging.user.twitter_name, user_path(user_tagging.user.twitter_name)

